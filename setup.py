from setuptools import setup, find_packages

setup(
    name='samurai',
    version='0.4.0', 
    author='NoW0NDER',  
    author_email='nowonder.dw@gmail.com', 
    packages=find_packages(), 
    description='侍，待命', 
    long_description=open('README.md').read(), 
    long_description_content_type='text/markdown', 
    install_requires=[],  
)
